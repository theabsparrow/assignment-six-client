import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard - Daily Dish",
  description:
    "This is the dashboard of the user. from here user can monitor of all his activities",
};

const UserDashboard = () => {
  return <div>this is userDashboard</div>;
};

export default UserDashboard;
